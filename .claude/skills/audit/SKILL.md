---
name: audit
description: Alias for /daily-summary — the AUDIT phase. Use whenever the operator says "audit" instead of "daily-summary"; both names are correct.
---

This is an alias. The phase name is AUDIT; the underlying skill is `daily-summary` (named after
its typical cadence). Read `.claude/skills/daily-summary/SKILL.md` in full and follow it exactly
— no argument needed. Do not duplicate or reinterpret its instructions here — this file exists
only so `/audit` and `/daily-summary` both work, matching the phase table in
`operations/README.md`.
