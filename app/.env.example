# Absolute path to the Sensai Compilar harness root (the folder containing engagements/).
# Defaults to the parent of this app/ directory when unset.
SENSAI_ROOT=/Users/nullzero/sensai-org/sensai-finder
