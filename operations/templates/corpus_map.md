<!--
Progressive Disclosure schema for INDEX-phase output (research_body/corpus_map.md).
Frontmatter fields are load-bearing: the gate hook parses `type` and `status` literally, and
requires a non-empty `## Baseline (As-Is)` section. This file is OVERWRITTEN on each /index run;
git history preserves prior snapshots for /longitudinal.
-->
---
type: corpus_map
status: ready
source: <comma-separated node filenames>
created: <ISO 8601 timestamp>
---

# Corpus Map

> **TL;DR:** <one-sentence characterization of what the corpus, taken as a whole, describes>

## Entity Index
- **<entity>** — appears in: [[node--a]], [[node--b]] — <one-line role across the corpus>

## Cross-Document Structure
<overlapping jurisdictions, shared definitions, compounding rules that exist implicitly ACROSS
documents — the relationships no single node states>

## Baseline (As-Is)
<the current state of the system/organization, established exclusively from the nodes; mark
undocumented areas explicitly as "not documented" — never filled from world knowledge>

## Coverage Gaps
<topics the strategic outcome needs that the corpus is silent on — feeds /analyze gap analysis>
